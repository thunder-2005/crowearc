{
  "name": "aml-shield",
  "version": "1.0.0",
  "private": true,
  "description": "AML Shield — Suspicious Activity Monitoring + SAR Documentation & Retention",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "seed": "npm --prefix backend run seed",
    "start": "concurrently -n BE,FE -c cyan,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start:backend": "npm --prefix backend run dev",
    "start:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
